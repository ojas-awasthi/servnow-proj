{
  "name": "service-marketplace-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Full Stack Service Marketplace and CRM Management Platform",
  "scripts": {
  "client": "npm --prefix client run dev",
  "server": "npm --prefix server run dev",
  "build": "npm --prefix client run build"
}
}